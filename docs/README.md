# Docs

- `ARCHITECTURE.md` — end-to-end explanation of the architecture and runtime flow.
- `SETUP.md` — installation + environment configuration + where data is stored.
- `PROMPTS.md` — example prompts for testing the agent.
